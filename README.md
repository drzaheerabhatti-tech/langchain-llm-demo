# LangChain LLM Demo

Simple Python script that calls OpenAI's `gpt-4o-mini` via LangChain, prints
the reply, and estimates token cost from `response.usage_metadata`.

Environment variables are loaded from `.env` (not committed to Git).
